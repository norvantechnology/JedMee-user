{
  "name": "medico-user-backend",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "scripts": {
    "dev": "STAGE=local PORT=4000 node local-server.js",
    "dev:dev": "STAGE=dev PORT=4000 node local-server.js",
    "dev:prod": "STAGE=prod PORT=4000 node local-server.js",
    "layer:install": "cd lib/nodejs-light/nodejs && npm install",
    "migrate": "node migrate.js",
    "migrate:local": "node migrate.js local",
    "migrate:dev": "node migrate.js dev",
    "migrate:prod": "node migrate.js prod",
    "migrate:local:seeds": "node migrate.js local --seeds",
    "migrate:dev:seeds": "node migrate.js dev --seeds",
    "migrate:prod:seeds": "node migrate.js prod --seeds",
    "migrate:dry": "node migrate.js local --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "//": "All Lambda runtime deps live in lib/nodejs-light/nodejs (the Layer). Only local dev-server deps go here so sam build's `npm install --production` ships a code-only function zip.",
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1040.0",
    "@aws-sdk/s3-request-presigner": "^3.1040.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "js-yaml": "^4.1.1",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^6.9.16",
    "pdfkit": "^0.15.2",
    "pg": "^8.16.3",
    "xlsx": "^0.18.5"
  }
}
